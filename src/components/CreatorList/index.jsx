import styles from "./creatorlist.module.css"

export default function CreatorList({ title, slogan }) {

    return (
        <div className="container">
            <div className={styles.box}>
                
                    

                    
            </div>
        </div>

    );
}